## Market-Adaptive CTA 利润质量改造进展
- 已完成三步利润质量改造并提交：
  - `bd78c8e`：最小预期 RR 过滤器（默认 `minimum_expected_rr=0.0`，可在配置启用 1.2）
  - `3fd4bbc`：首次 TP 从 50% 降至 25%，第二次 TP 维持 25%
  - `9c8a9a8`：`signal_flip` 改为分级退出，第一次先减仓，连续再次 flip 才全平
- 本轮复盘结论：已有 runner 单能拿住，但系统仍有太多手续费型薄单和刚开即失效单，主矛盾仍是入场质量不够厚。
- 用户已明确批准以"区块链合约专家 + 高级开发人员"的标准继续严谨优化系统：该加强的加强、该舍弃的舍弃，目标是做成优秀、干净、不冗余的交易系统。

## 2026-04-16 新推进
- 已完成第 1 步"精准收紧 relaxed / starter / early 入口"的低风险版本：
  - 新增 `relaxed_entry_minimum_expected_rr`
  - 新增 `starter_entry_minimum_expected_rr`
  - `relaxed_entry` 使用更高 RR 门槛；`starter / frontrun / scale_in / early_*` 再更高一档
- 针对性测试已通过；相关回归中仍仅剩两条既有漂移测试失败，非本次改动引入。
- 代码已提交：`163b615 feat(cta): tighten relaxed and starter entries`
- 因远端历史含 secret-scanning 阻挡提交，已改为基于 `origin/main` 生成干净分支并 cherry-pick 新改动：
  - 分支：`clean/fix/relaxed-entry-tighter`
  - 提交：`3762d1b feat(cta): tighten relaxed and starter entries`
  - 已成功 push，可从该分支发 PR。

## 2026-04-16 第 2 步：方向稳定性过滤
- 新增 `early_entry_direction_confirmation_bars` 配置（默认 2 根 K 线确认）
- 对 early_bullish / early_bearish / starter_frontrun 等入口，要求 swing SuperTrend 方向连续 N 根保持一致才能放行
- 新增 `_has_direction_confirmation()` 方法，用于验证方向连续性
- 已在 `starter_frontrun_ready`、`_resolve_early_bullish`、`_resolve_early_bearish` 逻辑中植入该确认
- 新增测试：
  - `test_cta_robot_blocks_early_bullish_when_swing_direction_just_flipped`：验证方向刚翻转时 block
  - `test_cta_robot_blocks_starter_frontrun_when_swing_direction_is_not_confirmed`：验证 starter_frontrun 方向未确认时 block
- 已提交 commit：`031eb00 feat(cta): require direction confirmation for early entries`
- 已成功 push 到远端同一分支

## 协作偏好（再次确认）
- 开发类任务继续遵循：先用一句话说明准备做什么，再分步骤、小步可回退推进；每一步完成后先汇报结果，再等用户确认是否继续下一步。
